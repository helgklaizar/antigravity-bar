# Project Context: Antigravity Bar

## Overview
A Swift-based macOS status bar application. The main source code and specific AI instructions are located in the `status-bar` directory.

## Stack
- Language: Swift
- Build System: Swift Package Manager (`Package.swift`)
- Platform: macOS (`.app` bundle)

## State
- [x] Base Swift package initialized
- [x] Application logic located in `status-bar/`

## Next Steps
- Refer to `status-bar/GEMINI.md` and `status-bar/TODO.md` for specific tasks.
